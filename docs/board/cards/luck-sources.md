# 행운 변동 소스 확장

> 유물·아이템·주사위를 넣을 때 따라오는 행운 소스. **단독으로 구현 중(WIP)인 작업이 아님.**

## 상태
아이디어 / 백로그 후보.

## 이미 있는 것
- `RunManager.luck` + `LuckResolver` — 동작 중
- `dice_triple_luck`(☘) — 굴림 후 면당 +1

## 나중에
새 콘텐츠(유물·직원·다른 주사위)를 넣을 때 그 티켓 안에서 luck 연동을 함께 정의하면 됨.
별도 `/feature`로 빼지 않는 한 `doing`에 두지 않는다.
