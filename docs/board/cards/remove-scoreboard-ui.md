# 숫자합 × 족보합 UI 제거

> 플레이 화면의 좌(숫자 합)·×·우(족보 합) 점수판 UI를 제거한다.

## 상세

- 대상: `run_scene`의 LeftPanel / MultiplyLabel / RightPanel 등.
- 점수 계산·연출 로직은 유지할지, UI만 숨길지 `/feature` 시 스펙에서 확정.

## 완료 조건

- [ ] 플레이 화면에 숫자합 × 족보합 패널이 보이지 않는다.
- [ ] 층 진행·굴림·점수 누적 플로우는 깨지지 않는다.
