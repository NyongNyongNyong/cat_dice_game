# Tasks: shop-purchase

## 구현

- [x] `dice_triple_l` / `dice_triple_v` + `change_to_lowest` / `change_to_missing`
- [x] `shop_offers.json` · `shop_offer_service.gd`
- [x] `RunManager.try_purchase_shop_replace`
- [x] `dice_shop_presenter` — 오퍼·로스터 dice 위젯, 2단계 클릭, Hover
- [x] `shop_scene` — OfferRow, RerollPreviewPresenter

## 검증

- [ ] 상점: H 구매 → 슬롯 교체 → 골드 차감
- [ ] L / V 굴림·점수 (에디터)
- [ ] Hover 6면 (오퍼·보유)
- [ ] `dice_catalog_spec_test.gd` headless
