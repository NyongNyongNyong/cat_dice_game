# Tasks: v0.2-board-luck

> 스펙: [systems/v0.2-board-luck.md](../systems/v0.2-board-luck.md)

## Phase A — 보드 + 배치

### 구현

- [x] `RunManager` 보드 모델: 12칸·4해금·8주사위 상한, `board_placement`, place/clear/unlock API
- [x] `RoundController`: 배치된 주사위만 굴리도록 연동, 배치 0개 굴림 차단
- [x] `run_scene`: 3×4 보드 그리드 + 로스터 트레이, **클릭 배치**
- [x] `run_scene`: **드래그 배치** — 트레이→칸 배치, 칸→칸 이동/교환, 칸→트레이 회수
- [x] `run_scene.tscn`: 보드(GridContainer)/트레이 노드 추가
- [x] `DiceSlot`: `set_locked`, `clear_dice_view`, `slot_kind`, `slot_dropped` (교차 컨테이너 드롭)
- [x] `RunManager.move_placed`: 칸↔칸 이동/교환

### AC (Phase A)

- [x] 시작 시 보드 4칸 해금, 보유 주사위 4개 트레이에 표시
- [x] 주사위를 해금 칸에 배치 후에만 굴림 가능 (배치 0개 레버 비활성)
- [x] 배치된 주사위만 굴림·족보·점수에 포함 (`get_placed_dice` → `dice_resources`)
- [x] 보유 주사위는 최대 8개 (`MAX_OWNED_DICE`)
- [x] 잠금 칸(5~12)에는 배치 불가
- [x] headless 씬 로드 + `board_spec_test` PASS

- [x] 드래그로도 배치·이동·교환·회수 가능, 클릭 배치도 유지 (`feature/board-drag-drop`)

> 구현 노트: 배치는 **클릭→클릭** + **드래그&드롭** 둘 다 지원. 트레이가 완전히 빈 경우 드롭 대상 칩이 없어 회수는 클릭으로 대체. 칩/레버/점수누적은 Phase B에서 행운으로 대체될 때까지 유지.

## Phase B — 행운

### 구현

- [x] `LuckResolver`: 미확정 주사위(≤4) `6^k` 전수 → 분포 → 행운 컷 → 선택
- [x] 굴림 플로우에 행운 통합 (`RoundController._roll_all_dice` → `LuckResolver.resolve`)
- [x] `RunManager.luck` 스탯(기본 0) + 행운 수치 UI 노출
- [x] 골드 리롤 경제 제거 (`can_afford_reroll`/`try_spend_reroll_gold`/`_reroll_selected_die`)
- [ ] (후속) 칩 제거 + 단발 굴림 재구성 — 굴림 횟수 제한 대체안 미정
- [ ] (후속) `reroll_preview_*` 스크립트·노드 완전 정리

### AC (Phase B)

- [x] 행운↑ → 낮은 점수 결과 제외, 행운↓ → 높은 점수 결과 제외
- [x] 행운이 매우 높아도 최저/실패 가능 (컷 0.9 점근, 100% 안전 불가)
- [x] `luck_resolver_spec_test` PASS (컷 공식 + 분포 바이어스)
- [x] 골드 리롤 코드 제거, 부팅 + 7개 스펙 PASS
- [~] 리롤/칩 **전면** 제거는 후속 (칩=굴림 제한 유지)
